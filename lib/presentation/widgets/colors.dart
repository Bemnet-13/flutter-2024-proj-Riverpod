import 'package:flutter/material.dart';

class CustomColors extends StatelessWidget {
  static const Color darkPrimary = Color(0xFF0097A7); // Dark Primary Color
  static const Color lightPrimary = Color(0xFFB2EBF2); // Light Primary Color
  static const Color primary = Color(0xFF00BCD4); // Primary Color
  static const Color textIcons = Color(0xFFFFFFFF); // Text / Icons
  static const Color accent = Color(0xFF448AFF); // Accent Color
  static const Color primaryText = Color(0xFF212121); // Primary Text
  static const Color secondaryText = Color(0xFF757575); // Secondary Text
  static const Color divider = Color(0xFFBDBDBD); // Divider Color
  static const Color scaffoldBackground = Color.fromARGB(255, 205, 221, 248);
  const CustomColors({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
